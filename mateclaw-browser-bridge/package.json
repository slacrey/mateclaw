{
  "name": "@mateclaw/browser-bridge",
  "version": "0.1.0",
  "description": "MateClaw Browser Agent Native Host — bridges Chrome Extension to Control Plane over WSS",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/cmd/bridge.js",
    "dev": "tsx watch src/cmd/bridge.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "ws": "^8.0.0",
    "yaml": "^2.0.0"
  }
}
