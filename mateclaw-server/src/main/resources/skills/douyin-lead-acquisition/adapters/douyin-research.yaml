kind: browser_research
target: douyin
inputs:
  - lead-search-request
outputs:
  - lead-record
limits:
  max_results: 30
  require_public_pages: true
notes:
  - Collect only public business signals.
  - Keep source URLs and observed timestamps with every lead.
